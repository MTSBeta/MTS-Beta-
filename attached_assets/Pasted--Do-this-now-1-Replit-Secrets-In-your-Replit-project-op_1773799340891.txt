

Do this now:

### 1. Replit Secrets

In your Replit project, open **Secrets** and add:

**Secret 1**

* Key: `GOOGLE_SERVICE_ACCOUNT_EMAIL`
* Value:

```text
replitpers@me-time-stories-beta-a.iam.gserviceaccount.com
```

**Secret 2**

* Key: `GOOGLE_PRIVATE_KEY`
* Value: paste the full `private_key` value from your JSON file exactly as it appears, including:

```text
-----BEGIN PRIVATE KEY-----
...
-----END PRIVATE KEY-----
```

### 2. In your app code

Use this pattern so the private key formats correctly:

```python
import os
from google.oauth2.service_account import Credentials
import gspread

SCOPES = [
    "https://www.googleapis.com/auth/spreadsheets",
    "https://www.googleapis.com/auth/drive",
]

service_account_info = {
    "type": "service_account",
    "project_id": "me-time-stories-beta-a",
    "private_key_id": os.environ.get("GOOGLE_PRIVATE_KEY_ID", ""),
    "private_key": os.environ["GOOGLE_PRIVATE_KEY"].replace("\\n", "\n"),
    "client_email": os.environ["GOOGLE_SERVICE_ACCOUNT_EMAIL"],
    "token_uri": "https://oauth2.googleapis.com/token",
}

creds = Credentials.from_service_account_info(service_account_info, scopes=SCOPES)
client = gspread.authorize(creds)

sheet = client.open_by_key("14qvEC3w_5_CbBy4yDNXBHkkCJi2cCvv33NOl9UWIC54")
worksheet = sheet.sheet1

print(worksheet.get("A1:B5"))
```

### 3. Better version if you also add the private key ID

Add one more secret in Replit:

* Key: `GOOGLE_PRIVATE_KEY_ID`
* Value:

```text
c7c02864706256a38d072ed11dc8e3ca940706e3
```

Then the code above is complete.

### 4. Install packages in Replit

Make sure these are installed:

```bash
pip install gspread google-auth
```

### 5. Share the Google Sheet

Open your Google Sheet and share it with:

```text
replitpers@me-time-stories-beta-a.iam.gserviceaccount.com
```

Set role to **Editor**.

### 6. Enable APIs

In Google Cloud for that project, make sure these are enabled:

* Google Sheets API
* Google Drive API

### 7. Your sheet ID

From the link you shared earlier, your sheet ID is:

```text
14qvEC3w_5_CbBy4yDNXBHkkCJi2cCvv33NOl9UWIC54
```

### 8. If Replit still errors

Use this checklist:

* service account email is shared on the sheet
* Sheets API enabled
* Drive API enabled
* private key pasted fully
* `.replace("\\n", "\n")` is in the code

Here is a cleaner Replit-ready snippet you can paste straight in:

```python
import os
import gspread
from google.oauth2.service_account import Credentials

SHEET_ID = "14qvEC3w_5_CbBy4yDNXBHkkCJi2cCvv33NOl9UWIC54"

SCOPES = [
    "https://www.googleapis.com/auth/spreadsheets",
    "https://www.googleapis.com/auth/drive",
]

service_account_info = {
    "type": "service_account",
    "project_id": "me-time-stories-beta-a",
    "private_key_id": os.environ.get("GOOGLE_PRIVATE_KEY_ID", ""),
    "private_key": os.environ["GOOGLE_PRIVATE_KEY"].replace("\\n", "\n"),
    "client_email": os.environ["GOOGLE_SERVICE_ACCOUNT_EMAIL"],
    "token_uri": "https://oauth2.googleapis.com/token",
}

credentials = Credentials.from_service_account_info(
    service_account_info,
    scopes=SCOPES
)

gc = gspread.authorize(credentials)
spreadsheet = gc.open_by_key(SHEET_ID)
worksheet = spreadsheet.sheet1

print("Connected successfully")
print(worksheet.get("A1:C5"))
```

