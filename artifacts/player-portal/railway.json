{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/player-portal build"
  },
  "deploy": {
    "startCommand": "npx serve artifacts/player-portal/dist -s -l 3000"
  }
}
